server:
  port: 8082

spring:
  application:
    name: portal-server

# 用于展示示例环境信息的基础元数据
app:
  description: Portal service providing system list, menus, and ticket-based jump URLs
  owner: DHGX Team
